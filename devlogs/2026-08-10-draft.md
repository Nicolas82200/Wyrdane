# Draft devlog — publication prévue le 2026-08-10

Notes brutes, pas de mise en forme. Une entrée par session, ajoutée en fin de fichier.

## Notes de session (au fil de l'eau)

## 2026-08-04

- Ajout d'un son (`AudioManager.RESOURCE_PLAYED`, réutilise `confirm.wav`) joué à chaque pose de carte-ressource (`Battle.play_resource_card`), pour joueur/IA/adversaire réseau.
- Animation de la boule voyageant vers le pool de mana (`AnimationSystem._travel_light_orb`) agrandie et retravaillée en effet de nuage de gaz (halo à trois couches qui "respire" pendant le trajet, plus un pouf de dissipation à l'arrivée sur le pool via `_puff_dissipate`) au lieu du simple point lumineux précédent.
