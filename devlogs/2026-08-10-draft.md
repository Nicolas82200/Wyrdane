# Draft devlog — publication prévue le 2026-08-10

Notes brutes, pas de mise en forme. Une entrée par session, ajoutée en fin de fichier.

## Notes de session (au fil de l'eau)

## 2026-08-04

- Amélioration de la visibilité du logo de type de carte (Éphémère/Rituel/Enchantement) et de l'icône de rangée (Avant/Arrière/Hybride) sur les cartes : ajout d'un badge circulaire coloré derrière l'icône, teinté selon la race de la carte (Démon : fond bordeaux + logo rouge vif, Abomination : fond vert foncé + logo vert clair, Humain : fond doré foncé + logo or vif, Mort-Vivant : fond gris + logo gris clair)
- Reprise du travail sur `0216-arena-mode-prototype` (prototype Arena/Battle Royale solo, 1 joueur + 7 bots) : merge de `dev` dans la branche (10 commits accumulés depuis la dernière synchro du 2026-08-02 : passe d'éligibilité Démon des Incantations, bouton de signalement, correctif de ré-attaque en boucle côté `CombatSystem`). Conflits limités à `README.md` (roadmap) et à l'ancien brouillon `devlogs/2026-08-03-draft.md`, entre-temps publié et archivé côté `dev` (`archive/2026-08-03-devlog-2.md`) — brouillon supprimé de cette branche plutôt que fusionné, contenu déjà publié.
- Validation post-merge : `.translation` recompilés depuis `game.csv` fusionné (`godot --import`), suite GUT complète relancée (624/624 tests passent), et boot headless du jeu réel (scène principale, autoloads chargés) sans erreur de compilation sur les scripts Arena/Battle/MainMenu. Corrigé au passage un fichier `.uid` manquant pour `tests/unit/test_arena_full_match_fuzz.gd` (généré à l'import mais jamais commité).
- Comblé un des écarts listés dans README « État actuel du prototype » : verrouillage de case boutique au reroll (design d'origine, retiré du prototype initial). `ArenaPlayerState.shop_locked` (parallèle à `shop_offer`) + `toggle_shop_lock()` ; `ArenaMatch._refresh_shop_offer(player, keep_locked)` ne redemande une carte au pool que pour les cases non verrouillées lors d'un `reroll()`, alors qu'une nouvelle manche (`start_shop_phase`) repart toujours d'une offre entièrement déverrouillée. Acheter une case verrouillée lève automatiquement son verrou. Nouvelle icône cadenas (`ArenaIcon.Kind.LOCK`) et petit bouton en coin de chaque `ArenaShopCardSlot` (au-dessus de la `Card`, intercepte le clic avant le drag d'achat). 5 nouveaux tests (`test_arena_match_shop.gd`) : préservation au reroll, remplacement des cases non verrouillées (pool délibérément vidé pour distinguer verrouillage réel de coïncidence), reset à la nouvelle manche, levée du verrou à l'achat, impossibilité de verrouiller une case vide. 629/629 tests passent après l'ajout.
- Deuxième écart comblé : timer de phase Boutique jugé trop court pour un vrai joueur humain (10s, contre 45s dans le design pour 8 joueurs réels — encore moins jouable avec la décision supplémentaire du verrouillage tout juste ajoutée). Vérifié que ce minuteur ne contraint que le joueur humain (les bots jouent leur manche plus tard, dans `_resolve_combat_phase`, après son expiration) : l'allonger n'a donc aucun effet sur leur comportement ni sur le déterminisme. Passé à 25s (compromis : plus confortable qu'avant sans viser les 45s pensés pour une vraie compétition d'achat à 8 humains, hors de propos ici). Timer de phase Combat (15s, affichage du résultat) laissé inchangé.
- Refonte du menu Paramètres : suppression de l'effet "fenêtre dans une fenêtre" (le panneau Paramètres, intégré à la fenêtre Actualités du menu principal, avait sa propre boîte flottante + voile sombre par-dessus). Le panneau prend maintenant toute la place disponible et affiche une navbar latérale (Audio / Graphismes / Contrôles) : on change d'onglet en un clic, sans quitter/rouvrir un sous-menu séparé.
- Les trois sous-menus (Audio, Graphismes, Contrôles) ont perdu leur propre fenêtre flottante (overlay + panneau bordé + bouton Retour) : ils ne sont plus que du contenu affiché dans la zone de droite de la navbar.
- Menu principal : retrait du compteur de victoires/défaites (V/D) toujours affiché en bas à gauche à côté du pseudo Steam — ce panneau n'affiche plus que pseudo, avatar Steam et monnaie. Le compteur V/D est désormais affiché dans la vue Profil (accessible en cliquant sur le pseudo/avatar).
- Suppression du bouton de debug "Refaire le tutoriel (debug)" du menu principal (et de sa clé de traduction associée).
- Vérification faite en instanciant les scènes SettingsMenu/MainMenu en headless (changement d'onglet, ouverture vue Profil/Paramètres) : aucune erreur de chemin de nœud après le refactor.

## 2026-08-06

- Correctif : `scenes/mainMenu/MainMenu.tscn` contenait des marqueurs de conflit Git non résolus (`<<<<<<<`/`=======`/`>>>>>>>` autour de `DeckCompPreviewCard`), laissés par un merge précédent — empêchait le chargement du menu principal au boot du jeu (`Parse Error` sur la scène, `LoadingScreen` plantait sur `.instantiate()` d'une ressource nulle). Résolu en gardant la version avec le conteneur intermédiaire `DeckCompPreviewHolder` (celle référencée par le chemin de nœud dans `scripts/mainMenu/MainMenu.gd`). Au passage, le brouillon de devlog de cette semaine avait le même problème (trois entrées du 2026-08-04 empilées avec des marqueurs de conflit imbriqués) — fusionnées en entrées distinctes.
- Vérifié par chargement headless (`load()` + `.instantiate()` de la scène) : plus d'erreur de parsing, la scène s'instancie correctement.

### 2026-08-04 (suite)

- Refonte de l'affichage Arena suite à un retour direct du joueur, comparée point par point à `Battle.tscn` (1v1) : portrait du héros passé de 90x125 à 135x187 (taille exacte de `PlayerHeroPanel` en 1v1, `HERO_PORTRAIT_SIZE`) ; l'or disponible déplacé de l'en-tête vers un petit panneau à côté du portrait du héros (au lieu d'être noyé parmi round/PV/niveau/XP) ; boutons Actualiser/Acheter XP regroupés dans leur propre panneau (`_make_panel_background`), plutôt que posés nus sur le décor.
- Bug de fond trouvé en comparant les coordonnées des rangées avec le 1v1 : `_make_lane_panel` imbriquait la rangée de serviteurs (`HBoxContainer`) DANS un `PanelContainer` à marges de contenu (8px de chaque côté) — en 1v1, le fond de rangée (`PlayerFrontLanePanel`, un simple `Panel`) et la rangée elle-même (`PlayerFrontLine`) sont deux nœuds SÉPARÉS superposés aux mêmes coordonnées, jamais imbriqués. Un `BoardMinion` fait 100x150 de haut pile la hauteur d'une rangée (150) ; l'imbrication dans un conteneur à marges le comprimait artificiellement. Remplacé par `_make_lane_background` (fond seul, `Panel` nu) + `_place_row` (rangée positionnée directement aux mêmes coordonnées, sans conteneur intermédiaire) pour les 4 rangées (boutique Avant/Arrière, plateau Avant/Arrière).
- Cartes de la boutique (`ArenaShopCardSlot`) : remplacé la `Card` de main affichée à l'échelle 0.34 (avec coût/texte visibles) par un vrai `BoardMinion` (même visuel qu'un serviteur déjà posé, sans aucun texte) — survoler affiche l'aperçu détaillé complet (`Card` agrandie), exactement le même mécanisme que pour un serviteur posé sur le plateau (`BoardMinion._on_mouse_entered`, qui sonde la position de la souris à chaque frame indépendamment de `mouse_filter`, donc fonctionne même avec `mouse_filter = IGNORE` posé pour laisser remonter le drag d'achat au parent). Le cadenas de verrouillage (case précédente) reste au-dessus, inchangé.
- 629/629 tests passent après la refonte (aucun test ne dépendait de la structure interne de `ArenaShopCardSlot`/`_make_lane_panel`, seulement du nombre d'enfants des rangées). Vérifié en jeu (scène Arena lancée directement, capture d'écran) : portrait agrandi, or à côté du héros, panneau reroll/XP structuré, survol d'une carte boutique affichant bien le détail complet sans texte sur la case elle-même.
>>>>>>> dev

### 2026-08-06 (suite)

- Trois retours du joueur sur la refonte Arena précédente. (1) Le survol des cartes boutique n'affichait en fait aucun détail malgré la vérification précédente : `mouse_filter = IGNORE` posé sur le `BoardMinion` pour laisser remonter le drag d'achat désactivait *aussi* son sondage de survol — `BoardMinion._process` exclut explicitement IGNORE de sa condition (`mouse_filter != MOUSE_FILTER_IGNORE`), erreur de lecture du code lors du correctif initial. Remplacé par `MOUSE_FILTER_PASS` (reçoit l'événement ET le laisse remonter au parent, contrairement à IGNORE qui ne le reçoit jamais, ou STOP qui le bloque) dans `ArenaShopCardSlot` et, même bug, dans `ArenaBoardMinionSlot` (serviteurs déjà posés — pas signalé par le joueur mais même cause exacte). (2) Portraits des autres participants (colonne de gauche) agrandis de 52x64 à 64x78. (3) Ajout d'un bouton engrenage (coin haut-droit, nouvelle icône `ArenaIcon.Kind.GEAR`) ouvrant le même `SettingsMenu.tscn` que le 1v1 (`show_quit = true` : bouton Concéder à la place de Fermer, seule façon de quitter une partie Arena en cours, pas de reprise).
- Session reprise sur une nouvelle branche (`0304-arena-hover-portrait-settings-fix`) : la branche `0216-arena-mode-prototype` avait déjà été mergée (PR #299) et son worktree nettoyé par une autre session pendant la mise en œuvre de ces 3 correctifs, perdant le travail non commité — reconstruit à l'identique par-dessus `dev` (qui contient déjà tout le travail précédemment mergé). 629/629 tests passent.

### 2026-08-06 (suite 2)

- Nouvelle vague de retours joueur sur l'Arena, tous traités sur la même branche `0304-...` :
  - **Or déplacé** de son emplacement précédent (à côté du portrait) vers l'emplacement exact du pool de mana du 1v1 (`ManaDisplay`, coin bas-droit, mêmes coordonnées).
  - **Bouton Prêt ajouté** (icône PLAY) : termine la phase Boutique immédiatement au lieu d'attendre l'expiration du minuteur. Vérifié que `SHOP_PHASE_DURATION` ne contraint que le joueur humain (les bots jouent après, dans `_resolve_combat_phase`) — cliquer Prêt n'a donc besoin d'attendre personne d'autre.
  - **Verrouillage par carte retiré**, remplacé par un gel de la boutique ENTIÈRE façon TFT (nouveau bouton flocon, `ArenaIcon.Kind.SNOWFLAKE`) : `ArenaPlayerState.shop_frozen` (bool unique, plus de tableau par case) ; `ArenaMatch.start_shop_phase()` saute entièrement le nouveau tirage si gelé (offre préservée telle quelle) puis consomme le gel (une seule manche, il faut regeler pour le prolonger) ; un reroll manuel dégèle et retire toujours une offre fraîche (payer un reroll ne doit jamais laisser l'offre inchangée). Tests de verrouillage par carte remplacés par leurs équivalents gel (préservation à la manche suivante avec pool vidé pour distinguer gel réel de coïncidence, consommation après une manche, dégel au reroll).
  - **Tooltips ajoutés partout** (`_make_stat` accepte maintenant un tooltip posé sur l'icône+chiffre entier, pas seulement le texte) : round, PV, niveau, XP, or, reroll (avec coût), acheter XP (avec coût), geler, prêt, paramètres — seule façon de comprendre une UI volontairement sans texte permanent.
  - Bug signalé à la défaite : `EffectManager._damage` plantait sur `Nonexistent function 'play_damage' in base 'RefCounted (SimAnimationSystem)'` pendant un dernier-souffle en combat simulé (dégât auto-infligé PACTE typiquement). En comparant la liste complète des méthodes `animation_system.*` appelées dans le code contre le stub `SimAnimationSystem` (combat headless Arena), 15 méthodes manquaient en tout (ajoutées côté 1v1 après l'écriture initiale du stub, jamais reportées ici) — toutes ajoutées en no-op par cohérence avec le reste du stub, pas seulement `play_damage`.
  - 629/629 tests passent après l'ensemble de ces changements ; suite complète relancée en ciblant spécifiquement `test_arena_full_match_fuzz.gd` (parties complètes multi-graines) pour confirmer l'absence de toute trace de `play_damage`/`Invalid call` dans la sortie — plus aucune occurrence. Deux avertissements sans rapport observés dans cette sortie ciblée (un bug connu du plugin GUT lui-même, et un message de nettoyage asynchrone pendant le teardown d'un test existant) : préexistants, non liés à ces changements.
